namespace IndustrialControls.Template.Models
{
    /// <summary>登录用户角色（模板占位，可按项目扩展）。</summary>
    public enum UserRole
    {
        Operator = 0,
        Engineer = 1,
        Administrator = 2
    }
}
